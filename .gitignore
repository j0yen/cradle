/target
**/*.rs.bk
Cargo.lock.bk
.DS_Store
# subagent isolation worktrees (Claude Code Agent --isolation=worktree
# creates these inside the slice; never part of the slice artifact)
.claude/worktrees/

# Per-model regenerable artifacts (PRD §4.7).
models/*/data/
models/*/checkpoint.safetensors
output/
